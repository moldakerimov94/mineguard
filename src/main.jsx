import React from "react";
import ReactDOM from "react-dom/client";
import GuardWorkstation from "../guard-workstation.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GuardWorkstation />
  </React.StrictMode>
);
